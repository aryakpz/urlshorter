import './App.css';
import { MainPage } from './components/MainPage';

function App() {
  
  return (
    <div className="App">
      {/* <p>
        {backendData?.users.map((user, i) => (
          <span key={i}>{user}</span>
        ))}
      </p> */}
      <MainPage/>
    </div>
  );
}

export default App;
   